import {View, Text} from 'react-native';
import React from 'react';
import {Presenter} from './types';

const LoginPresenter: React.FC<Presenter> = ({}) => {
  return (
    <View>
      <Text>LoginPresenter</Text>
    </View>
  );
};

export default LoginPresenter;
