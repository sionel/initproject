import {View, Text} from 'react-native';
import React from 'react';
import {Presenter} from './types';

const MapPresenter: React.FC<Presenter> = ({}) => {
  return (
    <View>
      <Text>MapPresenter</Text>
    </View>
  );
};

export default MapPresenter;
