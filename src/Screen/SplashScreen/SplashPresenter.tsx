import {View, Text} from 'react-native';
import React from 'react';
import {Presenter} from './types';

const SplashPresenter: React.FC<Presenter> = ({}) => {
  return (
    <View>
      <Text>SplashPresenter</Text>
    </View>
  );
};

export default SplashPresenter;
